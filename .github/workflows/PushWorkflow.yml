name: CI nssDMRG

on:
  push:
    branches: [main-ci]

permissions:
  contents: read

jobs:
  build:
    runs-on: ${{ matrix.os }}
    strategy:
      fail-fast: false
      matrix:
        os: [ubuntu-24.04, macos-15]

    steps:
      - uses: actions/checkout@v4

      - name: Install SciFortran release
        id: scifor
        uses: SciFortran/checkout@main

      - name: Check SciFortran environment
        run: |
          test -s "$SFROOT/lib/libscifor.a"
          pkg-config --exists scifor
          pkg-config --cflags --libs scifor
          echo "Using SciFortran $SCIFOR_RELEASE"

      - name: Configure nssDMRG
        run: cmake -S . -B build-ci -DUSE_MPI=ON -DBUILD_TESTING=OFF

      - name: Build nssDMRG
        run: cmake --build build-ci --parallel 2
